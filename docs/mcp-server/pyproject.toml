[project]
name = "dikong-mcp-server"
version = "0.1.0"
description = "FastMCP OpenAPI bridge for the Dikong API v2."
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.4.2",
    "httpx>=0.28.1",
]
