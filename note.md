界面
- 权限管理RBAC , 开发性，未来会新增角色
- 业务界面


priority
- 根据这个系统当前以及未来会涉及的用户设计权限管理RBAC
 - 实现 管理用户权限的页面
- 设计 api ，写相关文档
    - 任务管理，无人机，飞手（任务不由飞手创建，飞手负责接任务）
    - 规范：删除item，用标志位标记被删除



TDD ai 测试开发

测试脚手架
- 嵌入现有测试框架
- 一个地方记录中文描述的测试用例（下面工作量产生），一个地方存放测试代码。（由下面stage 4产生），这两者可对应起来
- 支持web界面浏览测试用例，用户可增删测试用例，相当于有一个测试case的增删改查系统
- 可调用git commit，记录测试用例的变更.(方便stage中的变更记录和回退决策技能使用)

Workflow设计
- 目标：用测试驱动 AI 开发，脚手架统一测试流程、测试样式、扩展机制。
- 材料：用户有一个业务描述的文件夹，模型只读权限。有一个开发记录文件夹，记录下每次迭代都commit和新增api记录和测试事件。有个每次迭代都可编辑的文件夹，记录每轮每个stage的产出。
- 概览：
- - Stage 0：
  - 输入：当前业务语义 + 已有 API。
  - 输出：
    - "应该开发的 API"候选清单（按保守策略排序，优先低风险、低耦合、可验证）。每次只新增小批 API。每次开始增量开发前，先评估新增 API 的风险和影响，确保每次迭代的变更范围可控。然后commit当前状态，进入增量开发阶段。
    - 状态码设计：种子技能在产出新增 API 候选时，同时必须给出业务状态码方案（至少包含成功、参数非法、权限不足、资源不存在、状态冲突、幂等重复请求等场景），并与后续 case 生成联动。
  - 版本策略：每次新增 API 都要求单独 commit，确保可回退。
- Stage 1：负责规范的代码实现，根据业务文件和stage 0新增api和状态码，实现接口。
- Stage 2：运行 server / 获取 API 列表，区分新增 API 与旧 API（有个地方专门记录api，初始情况为空）。
- Stage 3：结合业务与新增 API，生成新增业务流事件，写在文档里，供开发者编辑
  - 新 API 之间组合
  - 新 API 与旧 API 组合
- Stage 4：根据新事件生成 API 调用流：
  - 角色视角调用流（按 API 规范）
  - 业务视角调用流（含状态码、请求参数、关键响应）
  - 业务流用例（覆盖正常路径、边界条件、异常场景）
  - 根据测试脚手架的规范，生成对应的测试用例代码。
- Stage 5：基于调用流用脚手架生成并运行测试用例；同时回归运行历史用例。
  - 任一失败即打回开发阶段Stage1 重新实现代码，失败次数过多，跳到stage 7。
- Stage 6：成功后由 记录新增测试事件与测试用例，更新 changelog。commit·当前状态，表示这段迭出成功。
- Stage 7: 检测业务描述文件，和上面各个 stage 的产出文件，询问用户(search mode 不询问)是否要人为修改 stage 的产出， 是否有业务变更。如果修改了stage的产出，就跳到接受了那个修改了的产物的stage重新运行。
          如果有业务变更，判断是否需要回退到稳定状态重开开发（跳到skill 7），还是在当前状态继续增量开发（回到 stage 0）。（search mode自己决定，普通情况下用户决定）。
- Stage 8 回退决策技能：
  - 触发条件：业务语义变更（自己去看git记录分析一下业务描述文件夹有没有变），或新增业务需求。
  - 决策目标：判断“回退到稳定状态重开开发”还是“在当前状态继续增量开发”。
  - 输入：语义变更点、需求差异、当前分支状态、最近稳定 commit、测试失败分布、受影响 Skill 产物。
  - 输出：
    - 决策结论（rollback / continue）
    - 决策依据（风险、影响面、修复成本、回归成本）
    - 执行动作（回退到哪个 commit，或继续开发的最小变更集）
    - 就算回退，开发记录也不能丢失，必须保留变更记录和决策依据，作为后续分析和改进的输入。
    - 记录下回退这个行为到文档里，然后commit. 然后新开一个分支，在这个分支上revere到那个稳定 commit，继续开发。
  - 约束：若涉及核心业务语义冲突或历史用例系统性失效，默认倾向 rollback.



执行治理补充（开发者主导）
- 开发者可随时修改业务语义，及各个 Skill 的产物。
- 变更传播规则：
  - 若修改“业务语义”，按判断是否需要回退，并触发全链路重算。
  - 若修改“某 Skill 产物”（非业务语义），需要重跑该产物的下游相关环节及其依赖检查。
- 重跑完成前不得进入稳定状态。
- 最终提交门禁：必须由开发者人工拍板后，才能将当前状态 commit 为有效版本。
- 例外模式：允许开启“搜索模式（Search Mode）”。
  - 搜索模式下可进行信息检索、方案探索、草案生成，不需要开发者拍板。
  - 搜索模式允许直接 commit 状态（无需拍板）。
  - 但必须强制写清 commit log（变更原因、涉及技能产物、影响范围、回退点）。
  - 且必须写明记录责任技能（默认 Skill 0：API/Case 变动记录员；若非 Skill 0 执行，需注明替代记录技能）。
  - 新增规则：若“commit 流程本身”发生变化（门禁条件、模式规则、日志字段、责任技能分配等），必须同步记入文档（`note.md` + `tests_scaffold/README.md`）与 `tests_scaffold/CHANGELOG.md`。

补充约束（权限接口与用例生成）
- 权限接口侧能力收敛：只允许“创建用户”和“给用户赋予业务侧角色”。
- 不在权限接口中处理复杂业务动作（业务动作仍由业务接口承担）。
- 当“使用权限接口去生成测试 case”时，必须包含并显式标注以下限制态：
  - 未登录/无 token（401）
  - 已登录但无对应角色（403）
  - 角色正确且参数合法（成功路径）
  - 角色正确但参数非法（4xx 业务校验失败）
- 即：这些限制优先作用于 case 生成阶段，而不是只在最终执行阶段补测。


commit 规范：
- 要从git log标题看出是什么阶段，stage几